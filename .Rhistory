# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(ages)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=1)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=2)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals,ylim=c(0,max(totals)))
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals,ylim=c(0,max(totals)*0.3))
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals,ylim=c(0,max(totals)*1.5))
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals))
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals))
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals,cex=1.2)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=3,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals+1,pos=13,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals,pos=3,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages),y=totals,pos=1,labels = totals,cex=0.5,col = "Blue")
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages,col = "Blue")
text(x=barplot(totals,names.arg = ages),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages,col = "Blue")
text(x=barplot(totals,names.arg = ages),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages,col = "blue")
text(x=barplot(totals,names.arg = ages),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages,col = "blue")
text(x=barplot(totals,names.arg = ages,col="red"),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages,col="steelblue"),y=totals,pos=1,labels = totals,cex=0.5)
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages,col="steelblue"),y=totals,pos=1,labels = totals,cex=0.5,col="white")
# Changes and sets working directory
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
# loads given data set
df<-read.csv("Datasets/all_cities.csv")
# creates a sorted vector of all ages
ages<-sort(unique(df$age))
#creates vector containing total spending of each age
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
# plots a scatter plot of ages and their corresponding total
barplot(totals,names.arg = ages)
text(x=barplot(totals,names.arg = ages,col="steelblue"),y=totals,pos=1,labels = totals,cex=1,col="white")
