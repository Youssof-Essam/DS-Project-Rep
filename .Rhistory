dir<-getwd()
# df<-read.csv("C:/Users/Youssof Essam/Desktop/cities/all_cities.csv")
# ages<-sort(unique(df$age))
# totals<- as.numeric(vector())
#  for (age in ages) {
#    x=sum(df$total[df$age==age])
#    totals<-append(totals,x)
#  }
# plot(ages,totals)
#
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
# df<-read.csv("C:/Users/Youssof Essam/Desktop/cities/all_cities.csv")
# ages<-sort(unique(df$age))
# totals<- as.numeric(vector())
#  for (age in ages) {
#    x=sum(df$total[df$age==age])
#    totals<-append(totals,x)
#  }
# plot(ages,totals)
#
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
df<-read.csv("Datasets/all_cities.csv")
dir<-getwd()
dir<-gsub("R-Scripts","",dir)
setwd(dir)
df<-read.csv("Datasets/all_cities.csv")
ages<-sort(unique(df$age))
totals<- as.numeric(vector())
for (age in ages) {
x=sum(df$total[df$age==age])
totals<-append(totals,x)
}
plot(ages,totals)
